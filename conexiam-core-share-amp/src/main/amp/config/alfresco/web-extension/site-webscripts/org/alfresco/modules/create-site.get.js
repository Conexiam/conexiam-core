var sitePresets = [
   {id: "site-dashboard", name: msg.get("title.collaborationSite")},
   {id: "paid-engagement", name: msg.get("title.paidEngagement")}
];
model.sitePresets = sitePresets;

model.defaultVisibility = "PRIVATE";
